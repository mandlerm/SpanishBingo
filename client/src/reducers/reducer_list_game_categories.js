export default function(state = null, action) {
  return [
    { category: "sample" },
    { category: "food" },
    { category: "household" }
  ]
}

// switch (action.type) {
//   case "SET_CATEGORY":
//     return action.payload
// }
// return state
// }
